# Contributing

Use a focused branch for each change and keep commits small and descriptive. Open a pull request against `main` with a short summary and verification notes; avoid bundling unrelated changes.

Before opening a pull request, all required checks must pass:

```bash
npm run lint && npm test && npm run build
```
